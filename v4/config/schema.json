{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "tlswrapper configuration",
    "description": "Configuration file for tlswrapper. At least one of 'mux_listen', 'mux_connect', or entries in 'service.peers' should be present to create a config-driven tunnel or accept inbound mux connections.",
    "type": "object",
    "properties": {
        "type": {
            "description": "MIME type identifying the config format and version.",
            "type": "string",
            "default": "application/x-tlswrapper-config; version=4",
            "const": "application/x-tlswrapper-config; version=4"
        },
        "log": {
            "description": "Log output destination: 'stdout' (default), 'stderr', 'syslog', or 'discard'.",
            "type": "string",
            "enum": ["stdout", "stderr", "syslog", "discard"],
            "default": "stdout"
        },
        "no_redial": {
            "description": "Disable automatic redial for config-driven tunnels with mux_connect. Default: false.",
            "type": "boolean",
            "default": false
        },
        "api_listen": {
            "description": "Listen address for the HTTP management API (e.g. '127.0.0.1:9081').",
            "type": "string"
        },
        "mux_listen": {
            "description": "Address to accept inbound mux connections that create ephemeral tunnels.",
            "type": "string"
        },
        "mux_connect": {
            "description": "Address for the default config-driven tunnel to dial.",
            "type": "string"
        },
        "listen": {
            "description": "Local TCP address to accept application traffic on.",
            "type": "string"
        },
        "connect": {
            "description": "Forwarding target address for streams arriving from inbound ephemeral tunnels.",
            "type": "string"
        },
        "timeout": {
            "description": "Session ping timeout in seconds. Clamped to [5, 86400]. Default: 15.",
            "type": "integer",
            "minimum": 5,
            "maximum": 86400,
            "default": 15
        },
        "keepalive": {
            "description": "Keepalive probe interval in seconds. Clamped to [1, timeout]. Default: 25.",
            "type": "integer",
            "minimum": 1,
            "maximum": 86400,
            "default": 25
        },
        "send_timeout": {
            "description": "Send completion timeout in seconds. Clamped to [5, timeout]. Default: 15.",
            "type": "integer",
            "minimum": 5,
            "maximum": 86400,
            "default": 15
        },
        "idle_timeout": {
            "description": "Session idle timeout in seconds. 0 disables idle eviction; otherwise clamped to [5, 31557600]. Default: 0.",
            "type": "integer",
            "minimum": 0,
            "maximum": 31557600,
            "default": 0
        },
        "loglevel": {
            "description": "Log verbosity: 0=silence, 1=fatal, 2=error, 3=warning, 4=notice, 5=info, 6=debug, 7=verbose, 8=veryverbose. Default: 4.",
            "type": "integer",
            "minimum": 0,
            "maximum": 8,
            "default": 4
        },
        "max_sessions": {
            "description": "Maximum number of concurrent mux sessions. 0 means unlimited. Default: 128.",
            "type": "integer",
            "minimum": 0,
            "default": 128
        },
        "max_streams": {
            "description": "Maximum concurrent streams per session. 0 uses the internal default (1024). Default: 0.",
            "type": "integer",
            "minimum": 0,
            "default": 0
        },
        "max_startups": {
            "description": "Unauthenticated connection throttle in 'start:rate:full' format. Beyond 'start' connections, each new one is dropped with probability 'rate'% until the queue reaches 'full'.",
            "type": "string",
            "pattern": "^[0-9]+:[0-9]+:[0-9]+$",
            "default": "10:30:60",
            "examples": [
                "10:30:60"
            ]
        },
        "tls": {
            "description": "TLS configuration. All three of cert, key, and authcerts must be set together, or all must be absent (plaintext mode).",
            "type": "object",
            "properties": {
                "cert": {
                    "description": "PEM certificate. Use '@path' to read the file at startup, or provide an inline PEM string.",
                    "type": "string"
                },
                "key": {
                    "description": "PEM private key. Use '@path' to read the file at startup, or provide an inline PEM string.",
                    "type": "string"
                },
                "authcerts": {
                    "description": "Authorized peer certificates. Each entry is a '@path' reference or an inline PEM string.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "minItems": 1
                }
            },
            "required": ["cert", "key", "authcerts"],
            "additionalProperties": false
        },
        "mux": {
            "description": "Socket and flow-control settings for the mux (transport-level) connection.",
            "type": "object",
            "properties": {
                "tcp": {
                    "description": "TCP socket options for the mux socket.",
                    "type": "object",
                    "properties": {
                        "keepalive": {
                            "description": "Enable TCP keepalive on the mux socket. Default: false.",
                            "type": "boolean",
                            "default": false
                        },
                        "nodelay": {
                            "description": "Enable TCP_NODELAY on the mux socket. Default: true.",
                            "type": "boolean",
                            "default": true
                        },
                        "rcvbuf": {
                            "description": "Socket receive buffer size in bytes for the mux socket. 0 leaves the OS default unchanged. Non-zero values are clamped to [1, 2147483647]. Default: 0.",
                            "type": "integer",
                            "minimum": 0,
                            "maximum": 2147483647,
                            "default": 0
                        },
                        "sndbuf": {
                            "description": "Socket send buffer size in bytes for the mux socket. 0 leaves the OS default unchanged. Non-zero values are clamped to [1, 2147483647]. Default: 0.",
                            "type": "integer",
                            "minimum": 0,
                            "maximum": 2147483647,
                            "default": 0
                        },
                        "backlog": {
                            "description": "Listen backlog for the mux socket. Clamped to [1, 4096]. Default: 16.",
                            "type": "integer",
                            "minimum": 1,
                            "maximum": 4096,
                            "default": 16
                        }
                    },
                    "additionalProperties": false
                },
                "session_window": {
                    "description": "Fixed HTTP/2 connection-level flow-control window size in bytes. 0 leaves it unset. If either session_window or stream_window is non-zero, gRPC static flow control is used and any unset window stays at gRPC's default initial size. Non-zero values are clamped to [65535, 2147483647]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 2147483647,
                    "default": 0
                },
                "stream_window": {
                    "description": "Fixed HTTP/2 stream-level flow-control window size in bytes. 0 leaves it unset. If either session_window or stream_window is non-zero, gRPC static flow control is used and any unset window stays at gRPC's default initial size. Non-zero values are clamped to [65535, 2147483647]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 2147483647,
                    "default": 0
                },
                "max_halfopen": {
                    "description": "Maximum concurrent streams per mux session. 0 disables the explicit limit. Default: 256.",
                    "type": "integer",
                    "minimum": 0,
                    "default": 256
                }
            },
            "additionalProperties": false
        },
        "tcp": {
            "description": "Socket options for local (application-side) TCP connections.",
            "type": "object",
            "properties": {
                "keepalive": {
                    "description": "Enable TCP keepalive on local sockets. Default: false.",
                    "type": "boolean",
                    "default": false
                },
                "nodelay": {
                    "description": "Enable TCP_NODELAY on local sockets. Default: true.",
                    "type": "boolean",
                    "default": true
                },
                "rcvbuf": {
                    "description": "Socket receive buffer size in bytes for local sockets. 0 leaves the OS default unchanged. Non-zero values are clamped to [1, 2147483647]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 2147483647,
                    "default": 0
                },
                "sndbuf": {
                    "description": "Socket send buffer size in bytes for local sockets. 0 leaves the OS default unchanged. Non-zero values are clamped to [1, 2147483647]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 2147483647,
                    "default": 0
                },
                "backlog": {
                    "description": "Listen backlog for the local listener. Clamped to [1, 4096]. Default: 16.",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 4096,
                    "default": 16
                }
            },
            "additionalProperties": false
        },
        "service": {
            "description": "Service identity and per-peer config-driven tunnel routing settings.",
            "type": "object",
            "properties": {
                "id": {
                    "description": "Self identity announced in the handshake.",
                    "type": "string"
                },
                "peers": {
                    "description": "Peer identity to config-driven tunnel dial address mapping.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "listen": {
                    "description": "Peer identity to config-driven tunnel listen address mapping.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                }
            },
            "additionalProperties": false
        }
    },
    "additionalProperties": false
}